void main() {
  /**
   * Que es el pseudocodigo?
   * Es una forma de escribir codigo de forma humana para encontrar la solucion de un problema con el lenguaje humano.
   * Sirve para: pensar antes de programar, evitar errores logicos y tratar de conseguir los pasos de la solucion al pensar en como se puede resolver el problema si fuera algo manual.
   * El pseudo codigo se escribe claro, ordenado, paso a paso y con logica. Debemos pensar antes de programar para evitar errores
   * La estructura basica de un pseudocodigo es: entrada(leer), salida(mostrar), condicional(si condicion entonces hacer algo si no hacer otra cosa) bucle(para i desde 1 hasta 5 mostrar i), recorrido de listas(para cada elemento en la lista procesar elemento)
   * Siempre debe ser ordenado y entendible
   */

  //Ejercicios progresivos.
  //Ejercicio 1 escribe pseudocodigo para multiplicar dos numeros
  /**leer numero1
   * leer numero2
   * mostrar numero1 * numero2
   *
   * ejercicio2 contar cuantos numeros son pares en una lista
   * leer lista de numeros
   * para cada elementro en la lista procesar elemento
   * si el elemento % 2 == 0 entonces hacer
   * mostrar numero par si no hacer
   * mostrar numero impar.
   *
   * ejercicio3 calcular el promedio de una lista de numeros
   * leer listaNumero = [1,2,3]
   * leer sumatotal = 0
   * para i desde 0 hasta la longitud de la lista
   *  para cada elemento en la lista procesar elemento
   *    sumatotal += elemento
   *
   *ejercicio4 dada una lista: sumar todos los numeros, encontrar el numero mayor, encontrar el numero menor
   leer listaNumeros = [1,2,3,7,4,5]
   leer sumaTotal = 0;
   leer numeroMayor = [0]
   leer numeroMenor = [0]
   para cada elemento en la lista procesar elemento
    sumaTotal += elemento
    si el elemento es mayor al numeroMayor entonces hacer
     numeroMayor = elemento si no hacer
     si el elemento es menor al numeroMenor entonces hacer
      numeroMenor = elemento
   */
}
